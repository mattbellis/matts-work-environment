#!/usr/bin/python

#"Hello World" in Python. The above line must be the first in the file,
#also, it must end with LF (\n), not CRLF or CR. Sometimes /usr/local/bin/.
#Enter this into "hello.py" then type "python hello.py" or "./hello.py".
print "Hello World!"
#Make sure there is no DOS ^Z EOF mark here
