################################################################################
# README
################################################################################

To generate a grid of subfigures of the students do this:

* Download the Detail Class List to this directory. This produces a file 

    Detail Class List.html
 
and a directory.

    Detail Class List_files

* Rename the files directory (which contains the images) to remove spaces in the name.
For some reason, LaTeX doesn't like this. 

    mv Detail\ Class\ List_files Detail_Class_List_files

* Run the parser.

    python hello_world_web_scraper.py Detail\ Class\ List.html > temp.tex

* Build the pdf.

    pdflatex temp.tex

* View your class list!

    evince temp.pdf -f


